import {getServerSession} from "next-auth"; import {redirect} from "next/navigation"; import Link from "next/link"; import {authOptions} from "@/lib/auth"; import {db} from "@/lib/db";
export const dynamic="force-dynamic";
export default async function Dashboard(){const session=await getServerSession(authOptions);if(!session?.user?.id)redirect("/login");const user=await db.user.findUnique({where:{id:session.user.id},select:{name:true,email:true,locale:true,role:true,createdAt:true,lastLoginAt:true}});if(!user)redirect("/login");return <main><div className="container page-head"><div className="eyebrow">Account Dashboard</div><h1 className="title">Halo, {user.name||user.email}.</h1><p className="lead">Akun aktif, preferensi tersimpan, dan akses fitur pengguna dalam satu tempat.</p></div><section className="section" style={{paddingTop:20}}><div className="container grid grid-3"><div className="card"><div className="eyebrow">ACCOUNT</div><h2>{user.name||"User"}</h2><p className="muted">{user.email}</p><span className="chip">{user.role}</span></div><div className="card"><div className="eyebrow">LANGUAGE</div><h2>{user.locale.toUpperCase()}</h2><p className="muted">Locale tersimpan di akun.</p><Link href="/settings" className="btn btn-primary">CHANGE SETTINGS →</Link></div><div className="card"><div className="eyebrow">SECURITY</div><h2>Protected</h2><p className="muted">Session JWT dengan expiry, password hash, dan login provider.</p><Link href="/settings" className="btn btn-ghost">MANAGE ACCOUNT →</Link></div></div></section></main>}