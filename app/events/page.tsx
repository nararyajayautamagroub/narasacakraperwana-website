import Link from "next/link"; import {getLiveEvents} from "@/lib/server-data";
export const dynamic="force-dynamic";
export default async function Events(){const events=await getLiveEvents();return <main><div className="container page-head"><div className="eyebrow">Event System</div><h1 className="title">Events & Operations</h1><p className="lead">Convoy, tour, flight, voyage, expedition, internal event, dan community event.</p></div><section className="section" style={{paddingTop:20}}><div className="container grid grid-4">{events.length?events.map(e=><article className="card" key={e.id}><span className="status"><i/> {e.status}</span><h3 style={{fontFamily:"Space Grotesk",fontSize:20}}>{e.title}</h3><p className="muted">{e.division.name}<br/>{e.platform}<br/>{new Date(e.startsAt).toLocaleString("id-ID")}</p><p className="muted">{e.description}</p><Link href="/recruitment" className="btn btn-ghost">Register →</Link></article>):<div className="card"><strong>Belum ada event tersimpan.</strong><p className="muted">Tambahkan event melalui admin setelah authentication dan CRUD aktif.</p></div>}</div></section></main>}