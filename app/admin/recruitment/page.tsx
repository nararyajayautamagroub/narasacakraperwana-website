import {db} from "@/lib/db"; import {requireAdmin} from "@/lib/admin-auth"; export const dynamic="force-dynamic";
export default async function AdminRecruitment(){await requireAdmin();const rows=await db.recruitment.findMany({include:{division:true},orderBy:{createdAt:"desc"},take:100});return <main><div className="container page-head"><div className="eyebrow">Admin / Recruitment</div><h1 className="title">Applications.</h1></div><section className="section" style={{paddingTop:20}}><div className="container"><div className="table-wrap"><table className="table"><thead><tr><th>Nama</th><th>Username</th><th>Divisi</th><th>Simulator</th><th>Status</th><th>Tanggal</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.username}</td><td>{r.division.name}</td><td>{r.simulator}</td><td>{r.status}</td><td>{r.createdAt.toLocaleDateString("id-ID")}</td></tr>)}</tbody></table></div></div></section></main>}