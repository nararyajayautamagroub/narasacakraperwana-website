import {db} from "@/lib/db";
export default async function AdminPage(){const [applications,messages,events,fleet,news]=await Promise.all([db.recruitment.count({where:{status:"PENDING"}}),db.contactMessage.count({where:{handled:false}}),db.event.count(),db.fleet.count(),db.news.count()]);return <main><div className="container page-head"><div className="eyebrow">Admin</div><h1 className="title">Operations dashboard.</h1><p className="lead">Ringkasan operasional internal. Authentication dan RBAC wajib dipasang sebelum panel ini dibuka ke publik.</p></div><section className="section" style={{paddingTop:20}}><div className="container grid grid-4">{[["Recruitment Pending",applications],["Pesan Baru",messages],["Events",events],["Fleet",fleet],["News",news]].map(([label,value])=><div className="card stat" key={label as string}><span className="muted">{label}</span><strong>{value}</strong></div>)}</div></section></main>}