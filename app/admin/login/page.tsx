"use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function AdminLogin(){const router=useRouter();const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);
async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});if(r.ok)router.push("/admin");else{const d=await r.json().catch(()=>({}));setError(d.error||"Login gagal.");setLoading(false)}}
return <main><div className="container page-head"><div className="eyebrow">Secure Admin</div><h1 className="title">Admin login.</h1><p className="lead">Panel operasional dilindungi session HttpOnly. Password hanya dibaca server dari environment variable.</p></div><section className="section" style={{paddingTop:20}}><div className="container"><form className="card form" onSubmit={submit}><div className="field"><label>Admin password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></div>{error&&<div className="notice">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?"CHECKING...":"LOGIN →"}</button></form></div></section></main>}