import {requireAdmin} from "@/lib/admin-auth"; import {db} from "@/lib/db";
export const dynamic="force-dynamic";
export default async function AdminScraper(){await requireAdmin();const [sources,runs,items]=await Promise.all([db.scrapeSource.findMany({orderBy:{key:"asc"}}),db.scrapeRun.findMany({orderBy:{startedAt:"desc"},take:30}),db.scrapedItem.count()]);return <main><div className="container page-head"><div className="eyebrow">Admin / Scraper</div><h1 className="title">Scraper monitor.</h1><p className="lead">Sumber terkonfigurasi, histori eksekusi, dan jumlah item tersimpan.</p></div><section className="section" style={{paddingTop:20}}><div className="container grid grid-3"><div className="card"><div className="eyebrow">SOURCES</div><h2>{sources.length}</h2><p className="muted">Configured sources</p></div><div className="card"><div className="eyebrow">RUNS</div><h2>{runs.length}</h2><p className="muted">Recent scraper runs</p></div><div className="card"><div className="eyebrow">ITEMS</div><h2>{items}</h2><p className="muted">Stored scraped items</p></div></div><div className="card" style={{marginTop:18}}><h2>Sources</h2><div className="table-wrap"><table className="table"><thead><tr><th>Key</th><th>Status</th><th>Items</th><th>Last run</th><th>Error</th></tr></thead><tbody>{sources.map(s=><tr key={s.id}><td>{s.key}</td><td>{s.lastStatus||"NEVER"}</td><td>{s.lastItemCount}</td><td>{s.lastRunAt?.toLocaleString("id-ID")||"-"}</td><td>{s.lastError||"-"}</td></tr>)}</tbody></table></div></div></section></main>}