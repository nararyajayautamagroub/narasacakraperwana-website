"use client"; import {useState} from "react";
export default function ContactPage(){const [state,setState]=useState("idle");const [error,setError]=useState("");
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const form=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});if(r.ok){setState("done");e.currentTarget.reset()}else{const d=await r.json();setError(d.error||"Gagal mengirim pesan.");setState("error")}}
return <main><div className="container page-head"><div className="eyebrow">Contact</div><h1 className="title">Hubungi NCRP.</h1><p className="lead">Form resmi untuk pertanyaan umum dan kerja sama.</p></div><section className="section" style={{paddingTop:20}}><div className="container"><form className="card form" onSubmit={submit}><div className="field"><label>Nama</label><input name="name" required/></div><div className="field"><label>Kontak</label><input name="contact" required/></div><div className="field"><label>Subjek</label><input name="subject" required/></div><div className="field"><label>Pesan</label><textarea name="message" rows={7} required/></div>{state==="done"&&<div className="notice">Pesan berhasil dikirim.</div>}{state==="error"&&<div className="notice">{error}</div>}<button className="btn btn-primary" disabled={state==="sending"}>{state==="sending"?"MENGIRIM...":"KIRIM PESAN →"}</button></form></div></section></main>}