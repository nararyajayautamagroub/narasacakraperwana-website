import {db} from "@/lib/db";
export default async function GalleryPage(){const items=await db.galleryItem.findMany({orderBy:{createdAt:"desc"}});return <main><div className="container page-head"><div className="eyebrow">Gallery</div><h1 className="title">Dokumentasi komunitas.</h1><p className="lead">Event, convoy, penerbangan, pelayaran, dan ekspedisi virtual.</p></div><section className="section" style={{paddingTop:20}}><div className="container grid grid-3">{items.length?items.map(i=><figure className="card gallery-item" key={i.id}><img src={i.imageUrl} alt={i.alt}/><figcaption><strong>{i.title}</strong><span className="muted">{i.category}</span></figcaption></figure>):<div className="card"><strong>Galeri belum berisi media.</strong><p className="muted">Admin dapat menambahkan media setelah storage dipasang.</p></div>}</div></section></main>}