import {NextResponse} from "next/server"; import {db} from "@/lib/db"; export async function GET(){return NextResponse.json({news:await db.news.findMany({where:{published:true},orderBy:{publishedAt:"desc"}})})}