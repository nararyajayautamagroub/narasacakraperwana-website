import {NextResponse} from "next/server"; import {db} from "@/lib/db"; export async function GET(request:Request){const q=new URL(request.url).searchParams;const division=q.get("division")||undefined;const status=q.get("status")||undefined;const fleet=await db.fleet.findMany({where:{division:division?{slug:division}:undefined,status:status as any},include:{division:true},orderBy:{code:"asc"}});return NextResponse.json({fleet})}