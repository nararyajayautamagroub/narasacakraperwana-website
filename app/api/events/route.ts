import {NextResponse} from "next/server"; import {db} from "@/lib/db"; export async function GET(){return NextResponse.json({events:await db.event.findMany({include:{division:true},orderBy:{startsAt:"asc"}})})}