"use client"; import {useState} from "react"; import {divisions} from "@/lib/data";
export default function Recruitment(){const [sent,setSent]=useState(false);return <main><div className="container page-head"><div className="eyebrow">Recruitment Center</div><h1 className="title">Join the fleet.</h1><p className="lead">Pilih divisi dan simulator. Data yang diminta dibatasi pada kebutuhan komunitas, bukan koleksi biodata manusia sedunia.</p></div><section className="section" style={{paddingTop:20}}><div className="container"><div className="card">{sent?<div className="notice"><strong>Pendaftaran berhasil dikirim.</strong><br/>Contoh implementasi frontend aktif. Untuk produksi, hubungkan form ini ke backend/API dan database.</div>:<form className="form" onSubmit={e=>{e.preventDefault();setSent(true)}}><div className="field"><label>Nama</label><input required name="name"/></div><div className="field"><label>Username / Discord</label><input required name="username"/></div><div className="field"><label>Divisi</label><select required name="division" defaultValue=""><option value="" disabled>Pilih divisi</option>{divisions.map(d=><option key={d.slug} value={d.slug}>{d.name}</option>)}</select></div><div className="field"><label>Simulator</label><select required name="platform" defaultValue=""><option value="" disabled>Pilih simulator</option>{Array.from(new Set(divisions.flatMap(d=>d.platforms))).map(p=><option key={p}>{p}</option>)}</select></div><div className="field"><label>Pengalaman bermain</label><textarea rows={4} name="experience"/></div><div className="field"><label>Alasan bergabung</label><textarea rows={4} required name="reason"/></div><label style={{display:"flex",gap:9,alignItems:"center"}}><input type="checkbox" required/> Saya menyetujui peraturan komunitas.</label><button className="btn btn-primary" type="submit">SUBMIT APPLICATION →</button></form>}</div></div></section></main>}