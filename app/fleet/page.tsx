import {getLiveFleet} from "@/lib/server-data";
export const dynamic="force-dynamic";
export default async function Fleet(){const fleet=await getLiveFleet();return <main><div className="container page-head"><div className="eyebrow">Fleet Database</div><h1 className="title">Virtual Fleet</h1><p className="lead">Data armada dibaca langsung dari database operasional.</p></div><section className="section" style={{paddingTop:20}}><div className="container"><div className="table-wrap"><table className="table"><thead><tr><th>Fleet ID</th><th>Division</th><th>Unit</th><th>Type</th><th>Simulator</th><th>Status</th></tr></thead><tbody>{fleet.map(r=><tr key={r.id}><td>{r.code}</td><td>{r.division.name}</td><td>{r.unitName}</td><td>{r.type}</td><td>{r.platform}</td><td><span className="status"><i/>{r.status}</span></td></tr>)}</tbody></table></div></div></section></main>}