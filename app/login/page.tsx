"use client";
import {useState} from "react"; import {signIn} from "next-auth/react"; import Link from "next/link";
export default function LoginPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);
async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const r=await signIn("credentials",{email,password,redirect:false,callbackUrl:"/dashboard"});if(r?.ok){window.location.href="/dashboard"}else{setError("Email atau password salah.");setLoading(false)}}
function google(){setLoading(true);signIn("google",{callbackUrl:"/dashboard"}).catch(()=>{setError("Google Login belum dikonfigurasi di server.");setLoading(false)})}
return <main><div className="auth-shell container"><div className="card auth-card"><div className="eyebrow">Account</div><h1 className="title">Masuk.</h1><p className="lead">Gunakan email/password atau Google Login.</p><form className="form" onSubmit={submit}><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></div>{error&&<div className="notice">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?"MEMPROSES...":"LOGIN →"}</button></form><div className="auth-divider">atau</div><button className="btn btn-google" onClick={google} disabled={loading}>G  LOGIN WITH GOOGLE</button><p className="auth-link">Belum punya akun? <Link href="/register">Daftar sekarang</Link></p></div></div></main>}