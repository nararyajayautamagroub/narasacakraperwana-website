import {notFound} from "next/navigation"; import {db} from "@/lib/db";
export default async function NewsDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const n=await db.news.findUnique({where:{slug}});if(!n||!n.published)notFound();return <main><div className="container page-head"><div className="eyebrow">NCRP News</div><h1 className="title">{n.title}</h1><p className="lead">{n.excerpt}</p></div><section className="section" style={{paddingTop:20}}><div className="container"><article className="card article"><p className="muted">{n.publishedAt?.toLocaleString("id-ID")}</p><div className="article-content">{n.content}</div></article></div></section></main>}