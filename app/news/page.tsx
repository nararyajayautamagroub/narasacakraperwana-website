import Link from "next/link"; import {db} from "@/lib/db";
export default async function NewsPage(){const news=await db.news.findMany({where:{published:true},orderBy:{publishedAt:"desc"}});return <main><div className="container page-head"><div className="eyebrow">NCRP News</div><h1 className="title">Berita & update.</h1><p className="lead">Informasi resmi komunitas, perubahan operasional, dan pengumuman penting.</p></div><section className="section" style={{paddingTop:20}}><div className="container grid grid-3">{news.map(n=><article className="card" key={n.id}><div className="eyebrow">UPDATE</div><h2>{n.title}</h2><p className="muted">{n.excerpt}</p><Link className="btn btn-ghost" href={"/news/"+n.slug}>Baca berita →</Link></article>)}</div></section></main>}