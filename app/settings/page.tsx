"use client";
import {useEffect,useState} from "react"; import {useSession} from "next-auth/react"; import {locales,type Locale} from "@/lib/i18n"; import {LocaleSwitcher} from "@/components/LocaleSwitcher";
export default function SettingsPage(){const {data:session,status}=useSession();const [name,setName]=useState("");const [locale,setLocale]=useState<Locale>("id");const [currentPassword,setCurrentPassword]=useState("");const [newPassword,setNewPassword]=useState("");const [message,setMessage]=useState("");const [error,setError]=useState("");const [saving,setSaving]=useState(false);
useEffect(()=>{if(session?.user){setName(session.user.name||"");setLocale((session.user.locale||"id") as Locale)}},[session]);
if(status==="loading")return <main><div className="container page-head"><h1 className="title">Memuat...</h1></div></main>;
if(!session)return <main><div className="container page-head"><h1 className="title">Silakan masuk.</h1></div></main>;
async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setMessage("");setError("");const body={name,locale,currentPassword:currentPassword||undefined,newPassword:newPassword||undefined};const r=await fetch("/api/settings",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok){setError(d.error||"Gagal menyimpan.");setSaving(false);return}document.cookie=`ncrp_locale=${locale}; Path=/; Max-Age=31536000; SameSite=Lax`;setMessage("Pengaturan berhasil disimpan.");setCurrentPassword("");setNewPassword("");setSaving(false);window.location.reload()}
return <main><div className="container page-head"><div className="eyebrow">Account Settings</div><h1 className="title">Pengaturan.</h1><p className="lead">Kelola profil, bahasa, dan password akun.</p></div><section className="section" style={{paddingTop:20}}><div className="container"><form className="card form" onSubmit={save}><div className="field"><label>Nama</label><input value={name} onChange={e=>setName(e.target.value)} required minLength={2}/></div><div className="field"><label>Bahasa</label><select value={locale} onChange={e=>setLocale(e.target.value as Locale)}>{locales.map(x=><option key={x.code} value={x.code}>{x.flag} {x.name}</option>)}</select></div><div className="field"><label>Email</label><input value={session.user.email||""} disabled/></div><div className="settings-divider"/><div className="eyebrow">Change Password</div><div className="field"><label>Password saat ini</label><input type="password" value={currentPassword} onChange={e=>setCurrentPassword(e.target.value)} autoComplete="current-password"/></div><div className="field"><label>Password baru</label><input type="password" value={newPassword} onChange={e=>setNewPassword(e.target.value)} minLength={8} autoComplete="new-password"/></div>{message&&<div className="notice success-notice">{message}</div>}{error&&<div className="notice">{error}</div>}<button className="btn btn-primary" disabled={saving}>{saving?"MENYIMPAN...":"SAVE SETTINGS →"}</button><div><LocaleSwitcher current={locale}/></div></form></div></section></main>}