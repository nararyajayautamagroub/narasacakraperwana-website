import Link from "next/link"; import type {Division} from "@/lib/data";
export function DivisionCard({division}:{division:Division}){return <article className="card division-card"><div className="division-head"><span className="icon">{division.icon}</span><span className="chip">DATA LIVE</span></div><h3>{division.name}</h3><p>{division.description}</p><div className="chips">{division.platforms.map(x=><span className="chip" key={x}>{x}</span>)}</div><div className="bottom"><span className="muted">{division.members>0?division.members+" members":"Member count belum disinkronkan"}</span><Link href={"/divisions/"+division.slug} className="btn btn-ghost">View Division →</Link></div></article>}